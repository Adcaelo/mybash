#Bashrc spécifique Projet

git_envs=("cloud" "payment" "site" "www")

function ggit {

    curr_dir=$(pwd)

    for git_env in "${git_envs[@]}"
    do
        echo "${1^} de ${git_env^}"
        echo "cd /home/come/web/$git_env"
        cd /home/come/web/$git_env
        echo "git $@"
        git $@
        echo #new line
    done

    cd $curr_dir
}


function git_cleanup_all {

    echo "Cleaning all branches merged on master"
    read -r -p "Are you sure? [y/N] " response
    if [[ "$response" =~ ^([yY][eE][sS]|[yY])+$ ]]
    then

        for git_env in "${git_envs[@]}"
        do
            echo "processing ${git_env^}"
            cd /home/come/web/$git_env
            git checkout master
            git fetch
            if [[ $(git branch --merged | egrep -v "(^\*|master)") ]]; then
                git branch --merged | egrep -v "(^\*|master)" | xargs git branch -d
            fi
        done
    else
        echo "Cancelling"
    fi
}

function vmig {
    vim database/migrations/$(ls -t database/migrations/ | head -n1)
}


alias artisan="php artisan -vvv"
alias tinker="artisan tinker"

alias laravel_clean="artisan config:clear ; php artisan cache:clear ; php artisan route:clear ; php artisan view:clear"

alias monsql="mysql -uroot '-proot123!Root'"

alias web="cd ~/web/"
alias www="cd ~/web/digital-avocat/www/"
alias site="cd ~/web/digital-avocat/site/"
alias payment="cd ~/web/digital-avocat/payment/"
alias cloud="cd ~/web/digital-avocat/cloud"
alias admin="cd ~/web/digital-avocat/admin"
alias invoice="cd ~/web/invoice"

alias iban="echo 'FR1420041010050500013M02606' | xclip -i -selection clipboard"
alias siret="echo '53339106600024' | xclip -i -selection clipboard"

function pipeline {
    
    echo ""
    echo "npm run lint"
    npm run lint
    
    echo ""
    echo ""
    echo "composer fmt"
    composer fmt
    
    # read -n 1 -s -r -p "Press any key to continue"
    # echo ""
    # echo ""
    # echo "php unit"
    # phpunit
}

alias d_isup="docker ps"                                                   # What is up 
alias d_up="cd ~/web/backoffice-payment && docker-compose up -d"           # UP
alias d_stop="cd ~/web/backoffice-payment && docker-compose stop -d"       # STOP
# alias d_down="cd ~/web/backoffice-payment && docker-compose down -d"     # ERASE
alias d_sh="docker exec -it backoffice-payment sh"                         # Docker Prompt SH
alias d_ex="docker exec backoffice-payment "                               # Docker exec cmd

function d_help()
{
    echo "What is up"
    echo "alias = d_isup"
    echo "command = docker ps"
    echo ""

    echo "Start docker"
    echo "alias = d_up"
    echo "command = docker-compose up -d"
    echo ""

    echo "Stop docker"
    echo "alias = d_stop"
    echo "command = docker-compose stop -d"
    echo ""

    echo "Stop and erase docker"
    echo "alias = d_down"
    echo "command = docker-compose down -d"
    echo ""

    echo "Gimme an SH please"
    echo "alias = d_sh"
    echo "command = docker exec -it backoffice-payment sh"
    echo ""

    echo "Execute some commands"
    echo "alias = d_ex"
    echo "command = docker exec backoffice-payment"
}
